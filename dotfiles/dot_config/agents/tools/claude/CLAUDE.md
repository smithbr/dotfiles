# Global Claude Instructions

## Shared Rules
Read and follow all rules in `~/.config/agents/rules/`.

## Critical Rules (always enforce)
- Don't commit unless I explicitly ask.
- Don't push unless I explicitly ask.
- Do not add `Co-Authored-By` tags to git commits.
- Always prefer storing and retrieving secrets from 1Password over plaintext.
- Never store credentials, API keys, or tokens in source code or config files.
- Private things stay private. When in doubt, ask before acting externally.

## Chromium Browser
- Preferred Chromium browser: Comet (located in /Applications)

## Tools & Stack
OS: macOS, sometimes Debian
CLI: Ghostty and zsh
Package Management: Homebrew

## Workflow Preferences
- Prefer Homebrew for package installations whenever possible.
